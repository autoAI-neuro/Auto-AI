# 🚀 AUTOAI Backend Base

### 🔧 Requisitos
- Docker Toolbox o Docker Desktop
- VirtualBox habilitado

### ⚙️ Iniciar el proyecto
```bash
cd autoai
docker-compose -f infra/docker-compose.yml up --build -d
